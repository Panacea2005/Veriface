similarity:
  cosine:
    # Similarity threshold for face verification
    # Cosine similarity score must be >= this threshold to match
    threshold: 0.6
    method: max  # Aggregation method: "max", "mean", "median", "top_k", "delta_margin", "hybrid"
  euclidean:
    threshold: 1.23

# Anti-spoof detection threshold (no threshold, uses argmax)
